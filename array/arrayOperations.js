const arr = [23, 43, 55, 67, 89, 91];

arr.push(101); //push command is used to add a new element into the array
console.log(arr); 

//pop command is used to remove an element from last index position from an array
console.log(arr.pop());

console.log(arr.join(''));

console.log(arr.length);

arr2 = [23,34,45,67,89];
console.log(arr2.shift()); //shift
